USE [TpIS]
GO
/****** Object:  StoredProcedure [dbo].[s_Usuario_crear]    Script Date: 16/6/2023 11:52:02 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER PROCEDURE [dbo].[s_Usuario_crear](
	@id numeric(18,0),
	@user nvarchar(50),
	@password nvarchar(50),
	@active bit,
	@birthdate nvarchar(50)
	)

as begin

	INSERT INTO dbo.Usuario(id, username, password, active, birthdate) values (@id, @user, @password,  @active, @birthdate)
	exec s_Usuario_historico_crear @id= @id, @user= @user, @password=@password, @active=@active, @birthdate=@birthdate
end